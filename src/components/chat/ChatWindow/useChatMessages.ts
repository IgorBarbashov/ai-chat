import { useEffect, useState } from "react";
import type { ChatMessage } from "./ChatWindow.models";

const MOCK_MESSAGES: ChatMessage[] = [
  {
    id: "m1",
    chatId: "1",
    author: "Вы",
    variant: "user",
    text:
      "Привет! Расскажи, пожалуйста, подробнее про архитектуру **микросервисов**. Интересует:\n\n- основные принципы;\n- как сервисы общаются друг с другом;\n- какие протоколы чаще всего используются;\n- как обычно организуют *развёртывание* в продакшене.\n\nЕсли можно, приведи небольшой пример архитектуры.",
    createdAt: "2026-03-10T10:00:00Z",
  },
  {
    id: "m2",
    chatId: "1",
    author: "GigaChat",
    variant: "assistant",
    text:
      "Микросервисы — это архитектурный подход, при котором система разбивается на набор небольших, независимых сервисов, каждый из которых отвечает за ограниченную область ответственности. Обычно они общаются по лёгким протоколам (**HTTP/REST**, `gRPC`, очереди сообщений), разворачиваются независимо и могут масштабироваться по отдельности в зависимости от нагрузки на конкретную бизнес‑функцию.\n\nПростейший pseudo‑маршрут запроса может выглядеть так:\n\n```text\nClient -> API Gateway -> Auth Service -> Billing Service -> Notification Service\n```",
    createdAt: "2026-03-10T10:00:10Z",
  },
  {
    id: "m3",
    chatId: "1",
    author: "Вы",
    variant: "user",
    text:
      "А какие ключевые плюсы у такого подхода по сравнению с монолитом? Особенно интересуют:\n\n1. независимые релизы;\n2. горизонтальная масштабируемость;\n3. удобство работы разных команд над разными частями системы.\n\nМожно коротко в формате *списка*?",
    createdAt: "2026-03-10T10:00:20Z",
  },
  {
    id: "m4",
    chatId: "1",
    author: "GigaChat",
    variant: "assistant",
    text:
      "Основные плюсы микросервисной архитектуры можно свести к нескольким пунктам:\n\n- каждая команда развивает свой сервис независимо;\n- можно масштабировать только *узкие места*;\n- отказ одного сервиса, при правильной архитектуре, не приводит к падению всей системы;\n- проще экспериментировать с новыми технологиями в отдельных сервисах.\n\nКратко: **гибкость** и **масштабируемость** в обмен на более сложную инфраструктуру.",
    createdAt: "2026-03-10T10:00:30Z",
  },
  {
    id: "m5",
    chatId: "1",
    author: "Вы",
    variant: "user",
    text:
      "Звучит здорово, но, наверное, за всё это приходится чем‑то платить. Расскажи, пожалуйста, какие основные сложности появляются при работе с микросервисами: что происходит с **отладкой**, логированием, трассировкой запросов и согласованностью данных между разными сервисами?\n\nЕсли есть типичный пример, как выглядит *цепочка логов* для одного запроса — будет супер.",
    createdAt: "2026-03-10T10:00:40Z",
  },
  {
    id: "m6",
    chatId: "1",
    author: "GigaChat",
    text:
      "Да, у микросервисов есть и минусы:\n\n- сильно возрастает сложность инфраструктуры (нужны оркестраторы контейнеров, сервис‑дискавери, конфигурация сетей);\n- усложняется наблюдаемость — приходится настраивать централизованное логирование, метрики и распределённый трейсинг;\n- нужно аккуратно проектировать контракты между сервисами;\n- важно продумывать, как обеспечивать согласованность данных в условиях распределённой системы.\n\nДля многих систем разумный компромисс — начинать с **модульного монолита**, а выделять микросервисы по мере роста.\n\n```ts\n// Условный пример\nif (domain.isStable()) {\n  extractToMicroservice(domain);\n}\n```",
    createdAt: "2026-03-10T10:00:50Z",
    variant: "assistant"
  },
];

export const useChatMessages = (activeChatId: string) => {
  const [messages, setMessages] = useState<ChatMessage[]>([]);
  const [loading, setLoading] = useState(false);

  useEffect(() => {
    if (!activeChatId) return;

    setLoading(true);

    const timeoutId = setTimeout(() => {
      setMessages(MOCK_MESSAGES.concat(...(MOCK_MESSAGES.map((el, i) => ({...el, id: `t${i}`})))).filter((m) => m.chatId === activeChatId));
      setLoading(false);
    }, 500);

    return () => {
      clearTimeout(timeoutId);
    };
  }, [activeChatId]);

  return { messages, loading };
};
